# Sample connection properties for MySQL database.  The connectionUrl 
# points to a slave replica that may be checked for latency of updates. 

# Database connection information. 
url=jdbc:mysql://pelican/replica
user=cluster
password=secret
type=MySQL 5.0.22 Replication
replicaUrl=jdbc:mysql://puffin/replica

# Command to reformat/analyze tables. 
analyzeCmd=select 1
