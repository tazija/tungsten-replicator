# Sample connection properties for PostgreSQL database.  

# Database connection information. 
url=jdbc:postgresql://pelican/test2
user=cluster
password=secret
type=PostgreSQL 8.3
#reusedata=true
#replicaUrl=jdbc:postgresql://pelican/test2

# Command to reformat/analyze tables. 
analyzeCmd=vacuum full analyze
